# Server Configuration
PORT=4000

# Frontend CORS Origins (comma-separated)
FRONTEND_ORIGIN=http://localhost:3000

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-use-random-string
JWT_EXPIRATION=7d

# Default Super Admin Credentials (Change after first login!)
DEFAULT_ADMIN_PASSWORD=admin123456

# MongoDB Configuration (optional - if not set, uses JSON file storage)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority
