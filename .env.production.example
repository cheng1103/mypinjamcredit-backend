# Production Environment Variables
# 部署到Railway时需要配置这些环境变量

# Server Port (Railway会自动提供PORT)
PORT=4000

# Frontend URL (改为你的Vercel域名)
FRONTEND_ORIGIN=https://www.mypinjamcredit.com,https://mypinjamcredit.vercel.app

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=7d

# Admin Password (强烈建议修改！)
DEFAULT_ADMIN_PASSWORD=change-this-secure-password

# Node Environment
NODE_ENV=production

# ====== 如果要使用MongoDB (可选) ======
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/mypinjam?retryWrites=true&w=majority
# DATABASE_TYPE=mongodb  # 设为mongodb使用MongoDB，不设置则使用JSON文件
